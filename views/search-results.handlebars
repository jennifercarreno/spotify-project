<h1>Showing results for: {{search}}</h1>

    <div class="container mx-auto">
        <ol>
            {{#each tracks}}

           <li>
                <div class="row">

                {{!-- DISPLAYS ALBUM COVER --}}

               <div class="col-sm-1">
                {{#with this.album.images.[2]}}
                <img src={{this.url}}>
                {{/with}}
               </div>

               <div class="col-lg-6">
                {{!-- DISPLAYS TRACK NAME --}}
                   <h3>{{this.name}}</h3>

                {{!-- DISPLAYS ARTIST --}}
                   {{#each this.album.artists}}
                    <p>{{this.name}}</p>
                    {{/each}}
               </div>

                <div class="col-lg-3">
                    <form action="/playlist/choose" method="POST">
                    <input type="hidden" name="track_id" value="{{ this.id }}">
                    
                    <input type="submit" value="Add to playlist" class="btn">
                    </form>
                    
                </div>

                </div>

           </li>

            {{/each}}

        </ol>

    </div>



